{
  "changes": [
    {
      "type": "major",
      "comment": "For all tasks which run CLIs from dependencies (`tsc`, `prettier`, etc), make the binary resolution more resilient across package versions and run the binaries with `nano-spawn`",
      "packageName": "just-scripts",
      "email": "elcraig@microsoft.com",
      "dependentChangeType": "patch"
    }
  ]
}